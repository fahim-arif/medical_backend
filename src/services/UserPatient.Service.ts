import {FindOptions} from '@models/Common';

export const findPatients = (options: FindOptions) => {
  return;
};

  export const createAccount =  async () => {
    try {
      // const account = this.accountRepository.create(accountCreate);
      // return;
    } catch (error) {
      // throw new InternalError(error.message);
    }
  }
